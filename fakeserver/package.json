{
  "name": "fakeapi",
  "version": "1.0.0",
  "description": "fake endpoint api using json-webser",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch ./db.json",
    "start-auth": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "faker": "^4.1.0",
    "json-server": "^0.15.1",
    "jsonwebtoken": "^9.0.0"
  }
}
